import { EmptyLine } from "../../../models/lines";

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default function EmptyLineView({ line }: { line: EmptyLine }) {
  return <br />;
}
